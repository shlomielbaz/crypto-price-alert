# Add all of the environmental variables here instead of 
# embedding them directly in the app and utilize them 
# with the `DotEnv` package.

PORT=8080

# Database configurations
DB_HOST=localhost
DB_USER=shlomi
DB_PASSWORD=Antonio1!
DB_DATABASE=crypto_alerts


# UI configurations
UI_HOST=http://localhost:8081